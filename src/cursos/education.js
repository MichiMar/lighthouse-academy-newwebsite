import React from "react";

const Curriculum = () => {
  return (
    <div className="">
      <ol>link to montessori offcial website for each tool</ol>
      <ol>link to montessori offcial website for each tool</ol>
      <ol>link to montessori offcial website for each tool</ol>
      <ul>link to montessori offcial website for each tool</ul>
      <ul>link to montessori offcial website for each tool</ul>
      <ul>link to montessori offcial website for each tool</ul>
    </div>
  );
};

export default Curriculum;
